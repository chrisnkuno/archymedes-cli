---
description: Section, compartmentalize and optimize part of the codebase, tracked and rechecked
argument-hint: <scope, e.g. "packages/archymedes-cli", "archymedes.ts", "render/"> [goal]
---

Run a quality pass by following `docs/continuation/prompts/QUALITY_PASS.md` exactly. Read that file in full first.

Scope and goal: $ARGUMENTS

If no scope is given, use `packages/archymedes-cli` with the default goal.
