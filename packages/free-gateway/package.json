{
  "name": "@archymedes/free-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Hosted gateway for archymedes --free: holds one OpenRouter key server-side and serves only verified zero-priced tool models, with rate limits.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "start": "bun src/server.ts",
    "build": "bun build src/server.ts --target=bun --outfile dist/server.js"
  }
}
